# 디렉터리 구조 설명

이 문서는 세 가지를 설명합니다.

1. 두 에이전트 브랜치를 어떻게 합쳤는지
2. 지금 디렉터리 구조
3. 앞으로 지킬 규칙

---

## 1. 어떻게 합쳤나

### 합치기 전

```
main (e32508b)  state.py, graph_wiring.py, STATE_DESIGN.md
 ├── feat/domain-evaluation-agent   도메인 평가 에이전트 (커밋 3개)
 └── feat/stakeholder-agent         이해관계자 평가 에이전트 (커밋 1개)
```

두 브랜치는 같은 main 커밋에서 출발했지만 서로를 import하지 않았습니다.
웹 검색, 원문 수집, LLM 호출, 근거 형식이 모두 따로 구현되어 있었습니다.

| | 도메인 | 이해관계자 |
|---|---|---|
| 웹 검색 | Tavily | OpenAI Responses `web_search` |
| 원문 수집 | BeautifulSoup, PDF 지원 | HTMLParser, robots 검사 |
| 검색 결과 처리 | BM25 + dense 하이브리드 인덱스 | 원문 block 직접 검증 |
| LLM 호출 | LangChain | openai SDK 직접 |
| 기준 부모 State | `PipelineState` | `EvaluationState` (팀 설계서 v0.3) |

### 합친 순서 (`refactor/agent-layout` 브랜치)

1. **main에서 새 브랜치를 만들고 두 브랜치를 merge commit으로 합쳤습니다.**
   팀원 커밋이 그대로 남기 때문에, 이 브랜치가 main에 merge되면 기존 PR 두 개도 자동으로 Merged 처리됩니다.
2. **충돌 두 곳을 해결했습니다.**
   - `.gitignore`: 양쪽 항목을 모두 살렸습니다.
   - `requirements.txt`: 양쪽 패키지를 합쳤습니다. 겹치는 패키지는 더 좁은 버전 범위를 택했습니다. openai만 예외입니다(아래 "아직 정하지 않은 것" 참고).
3. **파일을 옮겼습니다.** `git mv`를 써서 파일 이력이 유지됩니다.
4. **코드는 두 가지만 바꿨습니다.**
   - import 경로를 새 위치에 맞게 고쳤습니다.
   - 각 에이전트의 "부모 State 변환" 부분을 `node.py`로 떼어내고 `make_node()`를 추가했습니다.

   검색·분석·검증 로직은 한 줄도 바꾸지 않았습니다. 기존 테스트 40개가 모두 그대로 통과합니다.

### 파일 이동표

| 이전 위치 | 새 위치 |
|---|---|
| `state.py` | `graph/state.py` |
| `team_state.py` | `graph/team_state.py` |
| `graph_wiring.py` | `graph/build.py` |
| `agents/domain_agent.py` | `agents/domain/subgraph.py` + `agents/domain/node.py`로 분리 |
| `agents/domain_state.py` | `agents/domain/state.py` |
| `prompts/domain.py` | `agents/domain/prompts.py` |
| `rag/`, `quality/`, `runtime/`, `evaluation/` | `agents/domain/` 아래로 |
| `stakeholder/agent.py` | `agents/stakeholder/subgraph.py` + `agents/stakeholder/node.py`로 분리 |
| `stakeholder/backend.py` 등 나머지 | `agents/stakeholder/` 아래로 |
| `run_stakeholder.py` | `scripts/run_stakeholder.py` |
| `tests/test_*.py`, `tests/fixtures/` | `tests/agents/<에이전트>/` 아래로 |
| `README.md` (이해관계자 문서) | `docs/STAKEHOLDER_AGENT.md` |
| `STATE_DESIGN.md` | `docs/STATE_DESIGN.md` |

### 삭제한 파일

- `stakeholder_agent.py`: `stakeholder/` 패키지를 한 파일로 합친 사본이었습니다.
  하지만 인용 길이 검사 같은 검증 로직이 패키지보다 오래된 버전이었고, 테스트는 패키지 쪽에만 붙어 있었습니다.
  그래서 패키지 쪽을 기준으로 남겼습니다.
- `prompts/__init__.py`: 안에 있던 파일이 도메인 폴더로 옮겨가면서 빈 패키지가 됐습니다.

---

## 2. 지금 구조

```
kongjinho/
├── 디렉터리 구조 설명.md    ← 이 문서
├── README.md
├── requirements.txt         두 에이전트 의존성 통합본
│
├── graph/                   ★ 팀 공유 영역 (여기만 공유)
│   ├── state.py             부모 State: PipelineState (최초 설계)
│   ├── team_state.py        부모 State: EvaluationState (팀 설계서 v0.3) + evidence reducer
│   └── build.py             add_node / add_edge / compile만
│
├── agents/                  에이전트별 구현 (서로 독립)
│   ├── domain/
│   │   ├── __init__.py      make_node, DomainAgentDeps만 노출
│   │   ├── node.py          부모 State → 내부 State 변환 → 서브그래프 실행 → 부모 State 형식으로 반환
│   │   ├── subgraph.py      plan_questions → retrieve → review_conditions → check_sufficiency → analyze
│   │   ├── state.py         도메인이 부모 State에 요구하는 확장 키
│   │   ├── prompts.py
│   │   ├── rag/             웹 검색, 원문 수집, 인덱스, 근거
│   │   ├── quality/         guard, linter, judge
│   │   ├── runtime/         실행 기록
│   │   └── evaluation/      검색 방식 비교 실험
│   │
│   └── stakeholder/
│       ├── __init__.py      make_node만 노출
│       ├── node.py          부모 State ↔ 내부 State 변환
│       ├── subgraph.py      plan → search → extract → review (→ research / revise) → finish
│       ├── models.py        내부 State, pydantic 모델
│       ├── backend.py       OpenAI 검색·구조화
│       ├── web.py           원문 수집, robots, 캐시
│       └── offline.py       API 없이 돌리는 fixture 백엔드
│
├── scripts/                 에이전트 단독 실행용
│   └── run_stakeholder.py   python -m scripts.run_stakeholder
│
├── tests/agents/
│   ├── domain/              LLM·네트워크 없이 도는 계약 테스트
│   └── stakeholder/         fixture 기반 오프라인 테스트
│
├── docs/                    에이전트별 설계 문서, State 설계
├── notebooks/               실행 기록 노트북
├── outputs/                 실행 산출물, 아키텍처 다이어그램
└── data/                    캐시 (git 제외)
```

### 한 에이전트 안에서 데이터가 흐르는 방식

```
부모 State ──▶ node.py ──▶ subgraph.py (내부 반복) ──▶ node.py ──▶ 부모 State 업데이트
             필요한 키만 추림                          자기 소유 키만 반환
```

`graph/build.py`는 에이전트 내부를 전혀 모릅니다. `make_node()`가 돌려주는 함수만 받아서 Edge로 연결합니다.

---

## 3. 규칙

### 에이전트 폴더

1. **한 에이전트의 구현은 전부 `agents/<이름>/` 안에 둡니다.**
   웹 검색, 원문 수집, 프롬프트, 품질 검사 모두 에이전트 폴더 안에 넣습니다.
2. **다른 에이전트 폴더를 import하지 않습니다.**
   `agents.domain`의 코드가 `agents.stakeholder`의 코드를 가져다 쓰면 안 됩니다. 반대도 마찬가지입니다.
   비슷한 코드가 있어도 억지로 합치지 않습니다. 세 에이전트 이상에서 똑같은 코드가 실제로 반복될 때 팀이 합의해서 공용으로 뺍니다.
3. **밖으로 내보내는 건 `make_node()` 하나뿐입니다.**
   `__init__.py`에서 `make_node`(그리고 필요하면 그 인자 타입)만 export합니다.
   그래프 쪽은 `from agents.<이름> import make_node` 외의 것을 import하지 않습니다.
4. **파일 역할을 나눕니다.**
   - `node.py`: 부모 State 변환만 담당
   - `subgraph.py`: 내부 반복 담당
   - `state.py` 또는 `models.py`: 내부 State 정의

### 노드 함수 (`node.py`)

5. **부모 State에서 필요한 키만 골라서 내부 State를 만듭니다.**
   다른 관점 에이전트의 결과(`market_findings` 등)는 프롬프트에 넣지 않습니다.
   관점끼리 서로의 결론에 물들지 않게 하기 위해서입니다.
6. **자기 소유 키만 부분 업데이트로 반환합니다.**
   `return {"domain_findings": ...}`처럼 반환합니다.
   입력 state를 직접 고치거나 `return {**state, ...}`처럼 전체를 돌려주지 않습니다.
   병렬로 도는 노드들이 같은 키를 동시에 쓰면 에러가 납니다.
7. **LLM, 검색 클라이언트 같은 런타임 객체는 `make_node()` 인자로 받습니다.**
   State에는 JSON으로 저장할 수 있는 값만 넣습니다.
8. **실패해도 예외를 밖으로 던지지 않습니다.**
   `completion.status`를 `partial`이나 `failed`로 표시하고, 이유를 `gaps`/`errors`에 담아 반환합니다.

### 공유 영역 (`graph/`)

9. **`graph/`는 팀 전체의 약속입니다.** 여기를 바꾸는 PR은 모든 에이전트 담당자의 리뷰를 받습니다.
10. **Edge는 `graph/build.py`에서만 정의합니다.** 에이전트 폴더 안에서 부모 그래프를 만들지 않습니다.
11. **두 개 이상의 노드가 쓰는 키에는 반드시 reducer를 붙입니다.**
    예: `evidence_store`, `errors`, `quality_by_perspective`
12. **근거와 주장의 ID에는 에이전트 이름을 접두사로 붙입니다.**
    예: `domain:claim:001`, `stakeholder:claim:003`
    근거 ID는 같은 출처면 항상 같은 값이 나오게 출처 정보로 만듭니다(URL + 위치 + 인용문 해시).

### 의존성, 테스트, 실행

13. **새 패키지는 `requirements.txt`의 해당 에이전트 구역에 추가합니다.**
    공통 패키지(langgraph, pydantic, httpx)의 버전 범위를 바꿀 때는 팀에 먼저 알립니다.
14. **테스트는 `tests/agents/<이름>/`에 둡니다.** API 키 없이 오프라인으로 돌아가야 합니다.
15. **단독 실행 스크립트는 `scripts/`에 두고, 루트에서 `python -m scripts.<이름>`으로 실행합니다.**
16. **산출물은 `outputs/<에이전트>/`에, 캐시는 `data/`에 둡니다.** 캐시와 API 키는 `.gitignore`에 넣습니다.
17. **에이전트 설명 문서는 `docs/<이름>_AGENT.md`에 둡니다.**
    입력 키, 출력 키, 실행 방법, 한계를 적습니다.

### 브랜치와 PR

18. **브랜치 이름은 `feat/<에이전트>-<내용>` 형식으로 짓고, main으로 PR을 올립니다.**
19. **PR 하나에서는 자기 에이전트 폴더만 수정합니다.** `graph/`나 `requirements.txt`를 바꿔야 하면 PR 설명에 그 이유를 적습니다.
20. **merge는 "Create a merge commit" 방식으로 합니다.** 커밋 작성자 기록을 남기기 위해서입니다.

### 새 에이전트를 추가할 때 체크리스트

- [ ] `agents/<이름>/`에 `__init__.py`, `node.py`, `subgraph.py`, `state.py` 만들기
- [ ] `__init__.py`에서 `make_node`만 export하기
- [ ] 부모 State에 자기 결과 키 추가하기 (`graph/` 변경이므로 팀 리뷰 필요)
- [ ] `graph/build.py`에 노드와 Edge 추가하기
- [ ] `tests/agents/<이름>/`에 오프라인 테스트 추가하기
- [ ] `docs/<이름>_AGENT.md` 작성하기

---

## 아직 정하지 않은 것

- **부모 State 통일:** `graph/state.py`(PipelineState)와 `graph/team_state.py`(EvaluationState) 중 하나로 정해야 합니다.
  지금은 도메인이 PipelineState를, 이해관계자가 EvaluationState를 기준으로 합니다.
  이해관계자는 `make_node(legacy=True)`로 PipelineState에도 붙일 수 있습니다.
- **근거 형식 통일:** 도메인은 `evidence_id`/`locator`를, 이해관계자는 `id`/`page_or_locator`를 씁니다.
  둘 다 `evidence_store`에 쓰려면 형식과 reducer를 하나로 맞춰야 합니다.
- **openai 버전:** 도메인 lock 파일은 `openai 3.x`, 이해관계자는 `openai<3` 기준으로 작성했습니다.
  통합 requirements에서는 상한을 풀었습니다. openai 3.16.2에서 오프라인 테스트 40개는 통과했지만,
  실제 API 호출(`responses.create`/`responses.parse`)은 아직 3.x에서 확인하지 않았습니다.
- **lock 파일:** `requirements.lock`(도메인)과 `requirements.lock.txt`(이해관계자) 두 개가 남아 있습니다.
