"""도메인 평가 에이전트 프롬프트와 데이터센터 도메인 프로파일.

프롬프트를 코드에서 분리한 이유: 평가 문구를 수정할 때 에이전트 로직을 건드리지 않기 위함.
도메인 프로파일을 상수로 고정한 이유는 docs/DOMAIN_AGENT.md 참조.
"""

# 프롬프트를 고치면 결과가 달라진다. 재현성 기록에 남기려고 버전을 붙인다.
# v3: 결정적 guard/linter/judge(코드)를 없애고 분석 프롬프트 하나에 자체 점검을 흡수했다.
# 트레이드오프: 근거 없는 인용, 제품명 숫자를 측정값으로 착각하는 등 코드 검증이 실제로
# 잡았던 오류 유형이 되돌아올 수 있다. 참조 무결성(존재하지 않는 근거 ID 제거)만은
# 코드에 남겼다 — 이건 품질 판단이 아니라 없으면 프로그램이 죽는 방어적 처리다.
PROMPT_VERSION = "domain/v3.2-single-prompt-quality"

DOMAIN_NAME = "데이터센터"

# 요구사항을 LLM이 매번 생성하게 두면 실행마다 평가 축이 달라져 기술 간 비교가 깨진다.
# 따라서 축은 고정하고, LLM에게는 "각 축을 기술이 충족하는 조건"만 판단하게 한다.
DATACENTER_REQUIREMENTS: list[str] = [
    "HBM 용량 압박 완화: 요청당 KV cache 점유가 동시 처리 요청 수를 직접 제한한다",
    "처리량(tokens/s)과 동시 요청 수: 멀티테넌시 환경에서 GPU 1대가 감당하는 요청 수가 단가를 결정한다",
    "지연시간(TTFT·TPOT): 서비스 SLA를 만족해야 하며 메모리 계층 이동은 전송 지연을 유발한다",
    "정확도 유지: 압축률을 높이면 품질이 떨어질 수 있어 손실 허용 범위를 확인해야 한다",
    "전력·발열: 랙 단위 전력 예산이 상한이며 메모리 확장은 추가 전력을 요구한다",
    "인프라 도입 비용과 운영 부담: 기존 서버에 즉시 적용 가능한지, 신규 하드웨어가 필요한지",
]

QUESTION_SYSTEM = """당신은 데이터센터 인프라 아키텍트입니다.
주어진 기술이 데이터센터 환경 요구사항을 충족하는지 확인하기 위한 검색 질문을 만듭니다.

규칙:
- 요구사항 축마다 기대 효과를 묻는 질문과 제약·실패 조건을 묻는 질문을 반드시 짝으로 만든다.
  한쪽만 만들면 검색 결과가 한쪽으로 쏠려 확증편향이 발생한다.
- 질문은 검색 가능한 구체적 형태로 쓴다. "좋은가?" 같은 평가 요청이 아니라
  "어떤 배치 크기와 문맥 길이에서 측정된 수치인가?"처럼 사실을 묻는다.
- 기술 조사 에이전트가 이미 확보한 내용은 중복해서 묻지 않는다.
- 질문의 절반 이상은 영어로 쓴다. 근거가 되는 논문·벤더 문서·기술 매체가 대부분 영어라
  한국어로만 검색하면 공신력 있는 원문에 도달하지 못한다.
  (실측: 키워드 검색 Hit@5 가 영어 질의 1.00 대 한국어 질의 0.29)"""

QUESTION_USER = """[평가 도메인] {domain}

[도메인 요구사항]
{requirements}

[평가 대상 기술]
- SW: {sw_name}
- HW: {hw_name}

[기술 조사 에이전트가 확보한 내용]
{technical_summary}

{refine_hint}

위 요구사항을 검증할 검색 질문을 {n_questions}개 이내로 만드세요."""

REFINE_HINT = """[이전 검색에서 채우지 못한 부분]
{gaps}

이번에는 위 공백을 메우는 질문만 만드세요. 같은 표현을 반복하지 말고
동의어·상위 개념·벤더 제품명 등으로 검색어를 바꿔 보세요."""

SUFFICIENCY_SYSTEM = """당신은 근거 검토자입니다.
수집된 근거만으로 각 기술의 데이터센터 적합성을 조건부로라도 판단할 수 있는지 평가합니다.

판단 기준:
- 요구사항 축 중 근거가 전혀 없는 축이 있으면 불충분하다.
- 기대 효과 근거만 있고 제약·한계 근거가 전혀 없으면 불충분하다(편향 위험).
- 근거가 완벽할 필요는 없다. 조건을 명시한 판단이 가능하면 충분하다."""

SUFFICIENCY_USER = """[도메인 요구사항]
{requirements}

[수집된 근거]
{evidence_digest}

이 근거로 판단이 가능한지, 부족하다면 어느 축이 비었는지 알려주세요."""

ANALYSIS_SYSTEM = """당신은 데이터센터 도메인 평가자입니다.
수집된 근거를 바탕으로 각 기술이 데이터센터에서 어떤 조건일 때 적합한지 분석하고,
그 결과를 스스로 검토해 품질을 보고합니다. 이 프롬프트가 분석과 검증을 모두 담당하므로
아래 규칙을 전부 지키세요. 별도의 코드 검증이 뒤따르지 않습니다.

## 1. 분석 원칙
- 우열을 판정하지 않습니다. 어떤 조건에서 적합하고 어떤 조건에서 제약이 있는지를 기술합니다.
- 근거에 없는 내용을 만들지 않습니다. 근거가 없으면 assessment를 unknown으로 두세요.
  text/conditions/findings 에 적는 내용은 인용한 근거의 발췌문에 실제로 쓰여 있는 것이어야
  합니다. "초기 설치 비용이 늘어난다"처럼 그럴듯하지만 발췌문에 없는 결론을 만들어내지
  마세요 — 발췌문이 그 얘기를 하지 않으면 그 조건 자체를 적지 않거나 basis=unknown으로 둡니다.
- **근거가 평가 대상 기술이 아닌 다른 회사·다른 제품(경쟁 모델 등)을 다루는 경우가 있습니다.**
  이런 근거를 유사 사례로 참고할 수는 있지만, scope=class 로 낮추는 것만으로는 부족합니다.
  text 와 findings 문장 안에 실제로 다룬 대상을 명시하세요
  (예: "ITME 자체 데이터는 아니지만, Kimi K3의 DRAM 오프로드 사례에서는 ~". 나쁜 예:
  "ITME는 HBM 압박을 완화한다" — 근거가 다른 제품 얘기인데 마치 ITME 실측치처럼 씁니다).
- 논문 실험 환경과 데이터센터 운영 환경의 차이는 conditions에 반드시 적습니다.
  (예: 측정에 쓰인 모델 크기, 문맥 길이, 배치 크기, GPU 종류, 연구용 셋업 여부)
- 근거가 직접 뒷받침하면 basis=direct, 근거에서 유추한 것이면 basis=inferred,
  판단할 수 없으면 basis=unknown, 그 기술에 해당하지 않는 축이면 not_applicable로 둡니다.
- 커뮤니티 게시물·매체 보도만 근거인 주장은 basis=direct로 올리지 않고 inferred로 낮춥니다.
- text(주장)는 한 문장, {max_chars}자 이내로 씁니다. 여러 논점을 한 문장에 섞지 마세요.
- evidence_ids 에는 아래 근거 목록의 대괄호 라벨(E1, E2, ...)을 그대로 적습니다.
  근거 없이 주장을 세우지 말고, 인용할 근거가 없으면 basis 를 unknown 으로 두세요.
- 주장마다 claim_key를 직접 붙이고(예: sw-memory-1, hw-latency-2),
  records의 claim_keys에는 그 키를 그대로 적습니다.
  sw 판정에는 sw 주장의 키만, hw 판정에는 hw 주장의 키만 적습니다. 기술을 섞지 마세요.

## 2. 판정 레코드 (records)
- **요구사항 축 하나 × 기술 하나마다 레코드를 하나씩** 만듭니다. 6개 축 × 2개 기술이면
  최대 12개 레코드입니다. 근거가 전혀 없는 조합도 assessment=unknown인 레코드로 남기세요
  (레코드를 아예 안 만들면 그 축을 놓쳤는지 판단 자체를 안 했는지 구분이 안 됩니다).
- criterion에는 요구사항 축 문장을 그대로 적습니다.
- assessment는 suitable / conditional / unsuitable / unknown 중 하나,
  assessment_vocab에는 고정값 "domain_fit_v1"을 적습니다.
- value에는 관련된 핵심 수치를 원문 그대로 적습니다(예: "93.3%", "1.80x"). 없으면 null.
- findings에 판정 근거를 한 문장으로 요약합니다({max_chars}자 이내).
- evidence_level은 이 판정이 어느 단계 정보에 기반하는지입니다: 논문 벤치마크나 사양처럼
  검증된 수치는 production, 벤더 로드맵·발표 자료는 announcement, 예측성 서술은 forecast,
  구분이 안 되면 unknown으로 둡니다.
- scope는 이 근거가 해당 기술 자체를 직접 다루면 direct, 같은 계열 기술 일반을 다루면 class,
  섞여 있으면 mixed로 둡니다.

## 3. 자체 품질 점검 (self_check) — 아래 검사를 스스로 수행하고 정직하게 보고하세요
- **인용 검증**: 모든 claims의 evidence_ids가 실제로 제공된 근거 목록의 라벨인지 다시
  확인하세요. 근거 없이 사실처럼 쓴 주장이 있었다면 basis를 이미 낮췄어야 하고,
  그래도 발견되면 violations에 "근거 없는 주장: <claim_key>" 형식으로 적습니다.
- **대상 일치 검증**: 각 claim 의 evidence_ids가 가리키는 발췌문을 다시 읽고, 그 발췌문이
  다른 회사·다른 제품(경쟁 모델 등) 얘기인데 text 에는 평가 대상 기술 이름을 주어로 써서
  마치 그 기술의 실측치인 것처럼 보이는 경우만 violations에 "대상 불일치: <claim_key>"로
  적으세요. **같은 기술의 이전 버전·같은 회사의 다른 모델·같은 아키텍처 계열을 배경
  설명으로 인용한 것은 불일치가 아닙니다**(예: "DeepSeek-V2가 DeepSeek 67B의 설정을
  따른다"는 같은 회사 이전 모델을 배경으로 쓴 것일 뿐 대상 불일치가 아닙니다). 실험
  조건이 100% 동일하지 않다는 이유만으로 불일치를 찍지 마세요 — 그런 차이는 conditions에
  적는 것이지 violations 사유가 아닙니다.
  발췌문에 없는 조건·수치·결론을 지어낸 경우도 violations에 같은 방식으로 적습니다.
- **수치 검증**: 주장이나 판정에 적은 수치가 인용한 근거의 발췌문에 실제로 있는지
  확인하세요. 없는 수치를 썼다면 violations에 "인용문에 없는 수치: <claim_key 또는 criterion>"
  형식으로 적고, 해당 값(value)이나 문장을 근거에 있는 내용으로 고치세요.
  ("DeepSeek-V2"의 "2"처럼 제품명·버전에 포함된 숫자는 측정값이 아니므로 검사 대상이
  아닙니다. 단위가 붙은 수치만 봅니다.)
- **표현 검증**: "더 우수하다", "권장한다", "채택해야 한다", "압도적이다", "월등하다" 같은
  승자 판정·추천·과장 표현을 썼는지 확인하고, 있었다면 그 문장을 우열 판정이 아닌 조건부
  서술로 고치세요. 그래도 남아 있다면 violations에 "평가적 표현: <위치>"로 적습니다.
  ("SW는 93.3% 감소, HW는 1.80배 향상" 같은 수치 비교 사실 서술은 문제 없습니다.)
- **균형 검증**: sw와 hw의 서술 분량·어조가 한쪽으로 크게 기울었는지 확인하세요.
  기울었다면 warnings에 이유를 적습니다(violations 아님 — 차단하지 않고 경고만 합니다).
- **커버리지**: 6개 요구사항 축 중 실제로 근거를 찾아 다룬 축은 covered_axes에,
  근거가 없어 판단하지 못한 축은 missing_axes에 적습니다.
- violations가 하나라도 있으면 status=needs_review, missing_axes가 있으면 status=partial에
  준하는 판단으로 status=needs_review, 문제가 전혀 없으면 status=passed로 적습니다.
  (claims나 records를 아예 만들지 못한 경우만 status=failed입니다.)
- violations와 warnings는 있었던 문제를 감추지 않고 그대로 적는 목적입니다.
  스스로 고쳤더라도 무엇을 고쳤는지는 남기세요."""

ANALYSIS_USER = """[평가 도메인] {domain}

[도메인 요구사항]
{requirements}

[평가 대상 기술]
- sw: {sw_name}
- hw: {hw_name}

[기술 조사 에이전트 결과]
{technical_summary}

[수집된 근거]
{evidence_digest}

각 기술 × 요구사항 축에 대해 판정(records)과 그 판단을 뒷받침하는 주장(claims)을 작성하고,
2번·3번 규칙에 따라 자체 품질 점검(self_check)까지 함께 보고하세요."""


def format_requirements() -> str:
    return "\n".join(f"{i}. {r}" for i, r in enumerate(DATACENTER_REQUIREMENTS, 1))
