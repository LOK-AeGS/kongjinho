# 최상위 의존성. 고정 버전은 requirements.lock 을 쓴다.
# 설치: python -m venv .venv && source .venv/bin/activate
#       pip install -r requirements.lock      (재현 실행)
#       pip install -r requirements.txt       (개발)

# 그래프와 LLM
langgraph>=1.0.1
langchain>=1.0.2
langchain-openai>=1.0.1
pydantic>=2.9
python-dotenv>=1.0.1

# 웹 검색·본문 수집
langchain-tavily>=0.2.12
httpx>=0.27
beautifulsoup4>=4.12
pdfplumber>=0.11.7

# 검색 색인 (긴 문서에만 사용)
rank-bm25>=0.2.2
faiss-cpu>=1.12.0
langchain-community>=0.4.1
langchain-huggingface>=0.3.0
sentence-transformers>=5.1.1

# 노트북
notebook>=7.4.5
