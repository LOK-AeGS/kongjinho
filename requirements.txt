# 설치: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# 도메인 평가 에이전트 실행에 필요한 최소 의존성
langgraph>=1.0.1
langchain>=1.0.2
langchain-openai>=1.0.1
pydantic>=2.9
python-dotenv>=1.0.1

# 노트북 데모용. 팀 공유 검색 도구가 준비되면 에이전트 자체에는 필요하지 않다
# (에이전트는 Retriever 프로토콜만 요구하고 인덱스 구축은 담당하지 않음).
notebook>=7.4.5
langchain-community>=0.4.1
langchain-text-splitters>=0.3.0
langchain-huggingface>=0.3.0
sentence-transformers>=5.1.1
faiss-cpu>=1.12.0
pdfplumber>=0.11.7
rank-bm25>=0.2.2
