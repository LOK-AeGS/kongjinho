# 최상위 의존성 (도메인·이해관계자 에이전트 통합).
# 설치: python -m venv .venv && source .venv/bin/activate
#       pip install -r requirements.txt

# 그래프 (공통)
langgraph>=1.0.1,<2
pydantic>=2.9,<3
httpx>=0.28,<1
python-dotenv>=1.0.1

# LLM
# 이해관계자: openai SDK 직접 사용 (Responses web_search)
# 도메인·보고서: LangChain 경유. langchain-openai 최신은 openai 3.x를 끌어온다.
# 이해관계자는 원래 openai<3 로 고정했으나 3.x 호환 여부를 확인해야 한다.
openai>=2.0
langchain>=1.0.2
langchain-openai>=1.0.1

# 도메인: 웹 검색·본문 수집
langchain-tavily>=0.2.12
tavily-python>=0.8,<1
beautifulsoup4>=4.12
pdfplumber>=0.11.7

# 도메인: 검색 색인 (긴 문서에만 사용)
rank-bm25>=0.2.2
faiss-cpu>=1.12.0
langchain-community>=0.4.1
langchain-huggingface>=0.3.0
sentence-transformers>=5.1.1

# 노트북
notebook>=7.4.5

# 보고서 PDF 출력 (agents/report/pdf.py)
reportlab>=4.4.9,<5
